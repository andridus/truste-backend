/*
var keys = keycharm({preventDefault: true});
keys.bind('tab', function(e){
	var id = R.findIndex(R.propEq('uid', window.PLAYER.uid))(window.players);
	if(id != -1){
		JogoChannels.send_a_message({target:{player: window.players[id]}});	
	}
}, 'keydown');
  */